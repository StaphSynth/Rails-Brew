<% provide :title, "Users" %>

<p id="notice"><%= notice %></p>

<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= render 'user', { user: user, size: 'large', link: true } %></li>
  <% end %>
</ul>
