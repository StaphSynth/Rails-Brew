<% provide :title, "Edit #{@recipe.name}" %>

<h1><%= "Editing #{@recipe.name}" %></h1>

<%= render 'form', { recipe: @recipe, ingredients: Ingredient.all } %>

<%= link_to 'Show', @recipe %> |
<%= link_to 'Back', recipes_path %>
