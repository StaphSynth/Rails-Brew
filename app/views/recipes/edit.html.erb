<% provide :title, "Edit #{@recipe.name}" %>

<h1><%= "Editing #{@recipe.name}" %></h1>

<!-- # <%= render 'form', recipe: @recipe %> -->

<%= react_component('RecipeForm', props: @props, prerender: false) %>
